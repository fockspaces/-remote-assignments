extends ../layouts/boilerplates.pug

block content
      .card
            .card-header
                  h5.card-title= article.title
                  h6.card-subtitle= article.authorName
            .card-body
                  p.card-text= article.content

            .card-footer.d-flex.justify-content-between
                  .col-sm-9
                        a(href=`/article` class="btn btn-primary") return  
                  .col-sm-2
                        .row.d-flex.justify-content-betwee
                              .col 
                                    a.btn.btn-success(href=`/article/${article.id}/edit`) edit
                              .col
                                    form#delete(action=`/article/${article.id}?_method=delete` method='POST')
                                          input.btn.btn-danger.mr-2(type='submit' value='Delete')